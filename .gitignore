# Video assets — too large for git (GitHub 100 MB limit)
*.insv
*.mov
*.avi
data/equirect/

# All MP4s except web-optimised versions in equi_video/
*.mp4
!equi_video/*_web.mp4

# Credentials / secrets — never commit these
cloudflare_r2_complete.json
upload_r2.py

# macOS
.DS_Store
